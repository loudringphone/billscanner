import * as React from 'react';
import { Text, StyleSheet, Center } from 'react-native';

export default function OnboardingUserScreen({ navigation }) {
    return(
        <Center safeArea>
        <Text style={styles.tesseract}>{image}</Text>
        <Button
        title="Go to Notifications"
        onPress={() => navigation.navigate('Notifications')}
      />
      </Center>
    )
}

const styles = StyleSheet.create({
    tesseract: {
      position: 'absolute',
      color: 'red',
    },
  })